export default function Hero() {
  return (
    <section
      id="hero"
      className="relative h-[400px] flex items-center justify-center bg-black bg-contain bg-center bg-no-repeat"
      style={{ backgroundImage: 'url(/s1.png)' }}
    >
    </section>
  );
}
